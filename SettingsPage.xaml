<?xml version="1.0" encoding="utf-8"?>
<Page
  x:Class="winui_local_movie.SettingsPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d">

  <ScrollViewer>
    <StackPanel Spacing="12"
                Margin="12">
      <TextBlock Text="设置"
                 Style="{ThemeResource HeaderTextBlockStyle}"/>
      <TextBlock Text="数据文件路径"
                 Style="{ThemeResource SubtitleTextBlockStyle}"
                 Margin="0,12,0,0"/>
      <TextBlock x:Name="ConfigPathText"
                 Text="正在加载路径信息..."
                 FontStyle="Italic"
                 TextWrapping="Wrap"
                 IsTextSelectionEnabled="True"/>
      <!-- 视频目录管理 -->
      <TextBlock Text="视频目录管理"
                 Style="{ThemeResource SubtitleTextBlockStyle}"
                 Margin="0,12,0,0"/>

      <StackPanel Orientation="Horizontal"
                  Spacing="8">
        <Button Content="选择目录"
                Click="SelectDirectory_Click"/>
        <TextBlock x:Name="SelectedDirectoryText"
                   Text="未选择目录"
                   VerticalAlignment="Center"/>
      </StackPanel>

      <ListView x:Name="DirectoriesListView"
                Margin="0,8,0,0"
                Height="200"
                SelectionMode="Single">
        <ListView.ItemTemplate>
          <DataTemplate x:DataType="x:String">
            <Grid>
              <TextBlock Text="{x:Bind}"
                         VerticalAlignment="Center"/>
            </Grid>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>

      <StackPanel Orientation="Horizontal"
                  Spacing="8">
        <Button Content="删除选中"
                Click="RemoveDirectory_Click"/>
        <Button Content="扫描视频"
                Click="ScanVideos_Click"/>
      </StackPanel>
      <TextBlock Text="上次扫描时间"
                 Style="{ThemeResource SubtitleTextBlockStyle}"
                 Margin="0,12,0,0"/>
      <TextBlock x:Name="LastScanTimeText"
                 Text="尚未扫描"
                 FontStyle="Italic"
                 TextWrapping="Wrap"/>
      <ProgressBar x:Name="ScanProgressBar"
                   IsIndeterminate="False"
                   Visibility="Collapsed"/>
      <TextBlock x:Name="StatusText"
                 Text=""
                 Margin="0,8,0,0"/>

      <TextBlock Text="云端备份"
                 Style="{ThemeResource SubtitleTextBlockStyle}"
                 Margin="0,16,0,0"/>
      <TextBox x:Name="BackupServiceUrlTextBox"
               Header="备份服务地址"
               PlaceholderText="例如：http://localhost:8080"
               TextChanged="BackupServiceUrlTextBox_TextChanged"/>
      <StackPanel Orientation="Horizontal"
                  Spacing="8">
        <TextBox x:Name="BackupNameTextBox"
                 Header="备份名称"
                 PlaceholderText="请输入备份名称"
                 Width="260"/>
        <Button Content="上传备份"
                VerticalAlignment="Bottom"
                Click="UploadBackup_Click"/>
      </StackPanel>

      <StackPanel Orientation="Horizontal"
                  Spacing="8">
        <Button Content="刷新备份列表"
                Click="RefreshBackups_Click"/>
        <Button Content="下载并恢复选中备份"
                Click="RestoreSelectedBackup_Click"/>
        <Button Content="删除选中备份"
                Click="DeleteSelectedBackup_Click"/>
      </StackPanel>

      <ListView x:Name="BackupsListView"
                Margin="0,8,0,0"
                Height="220"
                SelectionMode="Single">
        <ListView.ItemTemplate>
          <DataTemplate>
            <StackPanel Spacing="2">
              <TextBlock Text="{Binding DisplayName}"/>
              <TextBlock Text="{Binding Details}"
                         FontSize="12"
                         Opacity="0.7"/>
            </StackPanel>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackPanel>
  </ScrollViewer>
</Page>
