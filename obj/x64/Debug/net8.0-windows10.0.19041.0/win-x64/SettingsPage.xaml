<?xml version="1.0" encoding="utf-8"?>
<Page
  x:Class="winui_local_movie.SettingsPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d">

  <ScrollViewer>
    <StackPanel Spacing="12"
                Margin="12">
      <TextBlock Text="设置"
                 Style="{ThemeResource HeaderTextBlockStyle}"/>

      <!-- 视频目录管理 -->
      <TextBlock Text="视频目录管理"
                 Style="{ThemeResource SubtitleTextBlockStyle}"
                 Margin="0,12,0,0"/>

      <StackPanel Orientation="Horizontal"
                  Spacing="8">
        <Button x:ConnectionId='10' Content="选择目录"
                                             />
        <TextBlock x:ConnectionId='11' x:Name="SelectedDirectoryText"
                   Text="未选择目录"
                   VerticalAlignment="Center"/>
      </StackPanel>

      <ListView x:ConnectionId='2' x:Name="DirectoriesListView"
                Margin="0,8,0,0"
                Height="200"
                SelectionMode="Single">
        <ListView.ItemTemplate>
          <DataTemplate                      >
            <Grid x:ConnectionId='8'>
              <TextBlock x:ConnectionId='9'                
                         VerticalAlignment="Center"/>
            </Grid>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>

      <StackPanel Orientation="Horizontal"
                  Spacing="8">
        <Button x:ConnectionId='5' Content="删除选中"
                                             />
        <Button x:ConnectionId='6' Content="扫描视频"
                                        />
      </StackPanel>

      <ProgressBar x:ConnectionId='3' x:Name="ScanProgressBar"
                   IsIndeterminate="False"
                   Visibility="Collapsed"/>
      <TextBlock x:ConnectionId='4' x:Name="StatusText"
                 Text=""
                 Margin="0,8,0,0"/>
    </StackPanel>
  </ScrollViewer>
</Page>

